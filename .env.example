# Supabase (optional — app works with local product data without these)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Never commit real keys. Use Vercel / hosting env vars in production.
